{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "test": "eslint src && pulp test",
    "buildjs": "babel src/AffjaxSource.js --out-file src/Affjax.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "body-parser": "^1.15.2",
    "eslint": "^7.13.0",
    "express": "^4.14.0",
    "pulp": "^11.0.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.4",
    "xhr2": "^0.1.3"
  }
}
